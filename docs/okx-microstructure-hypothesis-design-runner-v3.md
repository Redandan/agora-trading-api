# OKX Microstructure Hypothesis Design Runner V3

Status: frozen offline Local Research tooling. V3 preserves the generic V2
scientific and filesystem behavior while refreezing runtime hash authority to
the smallest complete executable and scientific dependency set.

## Fixed zero-argument paths

- Source interpretation root:
  `C:/Users/Redan/.codex/local-research-node/outbox/local-node-microstructure-v3-interpretation-runner-v2`
- Source leaf: `interpretation-result.json`
- Coach proposal root:
  `C:/Users/Redan/.codex/local-research-node/inbox/local-node-microstructure-v3-hypothesis-design-runner-v3`
- Proposal leaf: `coach-proposal.json`
- Design output root:
  `C:/Users/Redan/.codex/local-research-node/outbox/local-node-microstructure-v3-hypothesis-design-runner-v3`
- Output leaf: `hypothesis-design-result.json`

The roots are distinct, pre-provisioned regular non-link directories. The CLI
accepts zero arguments and has no environment-selected path, task, contract,
tier, mechanism, disposition, or callable.

## Runtime trust root

The V3 self-task pins exactly ten repository inputs: Local task validation,
policy V3, canonical source bytes, interpretation core/schema/contract, and
the generic pure builder/design contract/result schema/proposal schema. It
does not pin an upstream runner, another lane, a test, documentation, or a
historical task. The task bytes remain an exact fixed authority checked before
and after the design operation.

The runner keeps equivalent local containment, regular-file, non-link and
Windows reparse-point checks without importing the broader handoff runner.
Concurrent implementation drift covers only the executable runner and the
proposal schema. Documentation and tests remain review evidence, never runtime
availability authority.

## Preserved scientific and publication boundary

The V1 proposal type/schema, pure builder, interpretation and hypothesis
contracts, result schema, four dispositions, selected-tier rule, source-bound
Coach proposal, `DESIGN_ONLY_NOT_REGISTERED`,
`CLOSED_NO_HYPOTHESIS_DESIGN`, exclusive create-once publication and exact
idempotent retry remain unchanged. Partial, linked, noncanonical, unsealed,
extra, source-mismatched, conflicting, or concurrently changed inputs fail
closed.

Historical V1 and V2 tasks, roots and documentation remain immutable and are
not migrated, renamed, reused, deleted, or repaired. Tests use deterministic
`TemporaryDirectory` fixtures only; the real fixed roots are not executed.

Real source/proposal execution, prediction, fees, slippage, fills, capacity,
PnL, drawdown, OOS value, candidate readiness, registration, activation,
deployment and liveness remain `MISSING_PROOF`. Immediate PnL and drawdown
effect are zero. This runner cannot write canonical or `.research-state`
state, call Research MCP, add a timer or writer, use network or paid APIs,
open OOS, or touch Trading, databases, orders, funds, SHADOW, PAPER, or LIVE.
