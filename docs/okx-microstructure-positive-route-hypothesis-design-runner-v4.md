# OKX Microstructure Positive-Route Hypothesis-Design Runner V4

Status: `LOCAL_RESEARCH_TOOLING_ONLY`

V4 preserves the positive-route V3 behavior while refreezing runtime hash
authority to the smallest complete executable and scientific dependency set.

## Fixed zero-argument interface

- Source:
  `C:/Users/Redan/.codex/local-research-node/outbox/local-node-microstructure-v3-interpretation-runner-v2/interpretation-result.json`
- Proposal:
  `C:/Users/Redan/.codex/local-research-node/inbox/local-node-microstructure-positive-route-design-runner-v4/coach-proposal.json`
- Output:
  `C:/Users/Redan/.codex/local-research-node/outbox/local-node-microstructure-positive-route-design-runner-v4/positive-route-hypothesis-design-result.json`

The roots are distinct, pre-provisioned regular non-link directories. The
production entry point accepts zero arguments and no environment-selected
root, task, contract, route, tier, proposal, builder, callable, disposition,
clock, fallback, or cross-lane input.

## Runtime trust root

The V4 self-task pins exactly eleven repository inputs: Local task validation,
policy V3, canonical source bytes, interpretation core/schema/contract, the
positive-route pure builder/design contract/result schema/proposal schema, and
the sole intraday route contract. It does not pin generic-lane files,
governance, tests, docs, historical tasks, handoff code, or an upstream runner.

Concurrent implementation drift covers only the executable positive runner
and its proposal schema. Documentation and tests remain review evidence, never
runtime availability authority. The self-task, every frozen runtime input,
source and proposal are revalidated before publication.

## Preserved route and publication boundary

The V2 proposal type/schema and pure builder, interpretation and design
contracts, result schema, four dispositions, selected-tier binding, route
`OKX_MICROSTRUCTURE_INTRADAY_ECONOMIC_ROUTE_V1`, frozen route hash and
`SOLE_PRIMARY` priority remain unchanged. Caller override, multiple routes,
DRA fallback, route switching after Design/Validation/OOS, registration, OOS,
activation, second writer, Trading and paid-API authority remain false.

The runner accepts one source-bound Coach envelope only for
`READY_FOR_ONE_HYPOTHESIS_DESIGN`; non-positive branches require an empty
proposal root. It publishes once by exclusive create and permits only an exact
validated idempotent retry. Missing, extra, linked, reparse, wrong-type,
noncanonical, unsealed, drifted, partial, source-mismatched or conflicting
bytes fail closed.

Historical V2/V3 tasks, roots and documentation remain immutable and are not
migrated, renamed, reused, deleted, or repaired. Tests use deterministic
`TemporaryDirectory` fixtures only; real fixed roots are not executed.

Prediction, fees, slippage, fills, capacity, matched-capital PnL, drawdown,
OOS value, candidate readiness, activation, deployment and liveness remain
`MISSING_PROOF`. Immediate PnL and drawdown effect are zero. This runner cannot
write canonical or `.research-state` state, call Research MCP, add a timer or
writer, use network or paid APIs, open OOS, or touch Trading, databases,
orders, funds, SHADOW, PAPER, or LIVE.
