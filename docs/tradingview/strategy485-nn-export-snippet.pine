// Instrumentation only. Add this immediately after nnOutput is calculated in
// a COPY of strategy 485. It does not change any strategy.entry condition.
plot(nnOutput, title = "NN Output Export", display = display.data_window, format = format.price, precision = 10)
