package com.agora.enums.trading;

public enum ExecutionRecommendation {
    HOLD,
    REVIEW_ONLY,
    SUGGEST_ENABLE,
    RISK_REDUCING_ONLY,
    BLOCK_NEW_ENTRY,
    APPROVAL_REQUIRED,
    IGNORE
}
